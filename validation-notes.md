# Responsive Web Validation Notes

The Campus Maintenance responsive web experience was inspected at a desktop viewport on 25 August 2026. The initial Student dashboard rendered the Campus Maintenance mark, role indicator, primary reporting action, request counters, request cards, and the four-tab navigation. The role-preview screen also rendered all required workspaces: Student, ICT Technician, Physical-Maintenance Technician, Security Officer, and Administrator.

The browser verification confirmed that the primary dashboard and role-selection entry point load from the responsive web route. The application’s deterministic test suite separately confirms the five-role model and category-to-team routing rules.

The upgraded responsive dashboard was also captured after the multilingual enhancement. The English, Shona, and Ndebele selector appears in the dashboard header; the screen maintains a clear primary maintenance-request action, compact operational status cards, category icons, enhanced request-card hierarchy, and the responsive tab navigation. The updated deterministic suite confirms all supported language catalogs contain the core navigation and request actions.

The production-readiness pass confirmed the new deep-navy service header, visible EN/SN/ND language control, role identity, refined tab navigation, live-account sign-in pathway, and photo-evidence intake. TypeScript and the deterministic suite pass, and a read-only database check confirms the `maintenance_requests` and `maintenance_updates` tables are present. Live persistent submission activates after a user completes the existing account sign-in flow.

The role-dashboard enhancement adds team-only dashboard states for ICT and Security, including a dedicated security-alert callout for urgent unacknowledged incidents. The evidence flow now has an image preview modal and progress feedback. The administrator queue supports status, priority, and team filters plus newest, oldest, priority, and pending-work sorting; deterministic tests cover urgent security filtering and both priority and pending sort orders.

The inspiration-driven refinement was visually verified on the responsive dashboard, request detail, and request queue. Request details now show a campus-location context surface and an active four-step progress rail with the current state distinguished from completed and upcoming stages. The role dashboards use action-focused hero surfaces for ICT and Security, while the administrator queue summarizes active filter and sort context in a Queue lens.

The primary student dashboard was rebuilt and visually verified as a visibly different shared experience: a deep-navy Campus Care hero, an overlapping operational command strip, and a tracking-feed report-card hierarchy replace the earlier simple heading, generic call-to-action, equal statistics row, and standard list treatment.

The subsequent enhancement was visually verified on both the primary dashboard and Activity tab. The Campus Care hero now includes an original abstract campus map with maintenance pin, active report cards include status-aware ETA badges, and the Activity tab uses a matching Service Pulse hero, command strip, and event-timeline card treatment.

The complete-application pass adds a dedicated welcome and secure sign-in entry screen, production-oriented notification preferences, and Help support. Technician-managed arrival times replace generic ETA rules and show in both report cards and request detail. Activity filters support all updates, assignments, progress, resolutions, and urgent events. The welcome, request-detail ETA, activity filters, preferences, and help screens render correctly; the external account provider flow remains available through the secure sign-in control and must be completed with an approved campus account.

The identity and location-data pass adds protected profile, notification-preference, and campus-building APIs. A signed-in account loads its persisted operational role to select the appropriate dashboard automatically, and notification toggles save to the same account. The dashboard map now queries the building-coordinate service and labels unverified geography clearly until the institution supplies an approved campus map or coordinates. The three related database tables were confirmed present; non-authenticated visual validation retains demonstration-mode behavior.

The website pass was verified at desktop and tablet widths. Desktop switches to a persistent deep-navy left workspace rail and wider Campus Care canvas; the dashboard introduces a Website Workspace briefing, Activity receives a website service-timeline context panel, the request queue uses the larger workspace controls and full-width cards, and Profile becomes a two-column account-and-settings page. Tablet retains the compact navigation and readable single-column workflows. TypeScript and deterministic tests remain passing.

The search-and-analytics pass adds a persistent desktop header search for request references, titles, categories, teams, locations, and approved building records. Request results honor the active role’s permitted dataset, while building results are available after sign-in through the protected map API. The administrator-only Analytics workspace derives SLA posture, completion rate, team workload, and at-risk counts from the same request data used by the operational workspace. The reusable Campus Maintenance delivery skill was validated with the skill validator.

The SLA and search-assistance pass adds configurable priority targets, persisted due timestamps for new authenticated requests, a red visible deadline state for approaching or breached request cards, and a UTF-8 analytics CSV download that mirrors the current dashboard summary and team metrics. The search header now shows direct auto-complete results and saves a short local recent-search history. The full deterministic test suite and TypeScript validation pass; the red SLA card treatment was visually verified on the request queue.

The backend completion pass adds server-side identity-aware request visibility, request ownership checks, technician-assignment identity storage, administrator-controlled building upsert support, and protected server-side analytics aggregation. Authenticated request creation now retains the server-generated reference in the shared workspace; subsequent assignment, status, progress, arrival, acknowledgement, and feedback actions refresh account-backed data after persistence. The reviewed additive `assigneeUserId` migration was applied successfully. The test suite reports four passing files and thirteen passing tests, with one existing logout test skipped; `pnpm check` passes. Unauthenticated preview log entries remain expected because no account session cookie is present.

The production-readiness pass retires seeded maintenance requests, preset staff names, local role switching, static client-side SLA target creation, specific example location fallbacks, and demonstration-role entry points. Registered institution accounts now provide staff names and role eligibility; administrators can configure account roles, compatible request assignments, verified campus locations, and official SLA targets from Operations configuration. New request submission requires authenticated access, a verified active location, and an administrator-configured SLA target. The reviewed additive `campus_sla_policies` migration was applied successfully. Final validation reports five passing test files with fifteen passing tests, one existing skipped logout test, and a successful `pnpm check`. Unauthenticated preview log entries remain expected without a session cookie.

The administrator governance pass adds persisted least-privilege staff permissions for user management, request assignment, campus locations, SLA targets, escalation rules, and analytics access. It adds one dedicated Staff Roles & Permissions dashboard and expands Service Policy with approved SLA targets plus per-priority escalation thresholds, active status, and operational recipient role. The protected backend rejects escalation recipients without an operational role and escalation thresholds outside their approved SLA window. The additive `campus_staff_permissions` and `campus_escalation_rules` migration was reviewed and applied successfully. Final validation reports five passing test files with seventeen passing tests, one existing skipped logout test, and a successful `pnpm check`.

The governance observability pass adds immutable administrator audit records for staff role changes, delegated permission updates, SLA target changes, escalation policy changes, and CSV import activity. Bulk CSV import validates required headers, role values, permission flags, duplicate rows, and a 100-row limit before an administrator reviews and submits records. Existing accounts receive the new role and permissions immediately; unregistered accounts are provisioned safely at their first approved sign-in. Governance analytics now uses persisted SLA deadlines and recorded resolution timestamps to present compliance by priority, current SLA breaches, active rules, and open escalation exposure. The reviewed additive audit-log, staff-provisioning, and `resolvedAt` migration was applied successfully. Final validation reports seven passing test files with twenty-two passing tests, one existing skipped logout test, and a successful `pnpm check`.
