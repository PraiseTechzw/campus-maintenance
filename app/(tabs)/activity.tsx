import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { FlatList, StyleSheet, Text, useWindowDimensions, View } from "react-native";
import { C, shadow } from "@/constants/campus";
import { roles, useMaintenance } from "@/lib/maintenance-store";
export default function Activity() { const { role, visible } = useMaintenance(); const { width } = useWindowDimensions(); const data = visible.flatMap((r) => r.activity.map((a) => ({ ...a, request: r.id }))).reverse(); return <View style={s.screen}><FlatList data={data} keyExtractor={(x) => x.id} contentContainerStyle={[s.content, width > 740 && s.wide]} ItemSeparatorComponent={() => <View style={{ height: 10 }} />} ListHeaderComponent={<View><Text style={s.eyebrow}>{roles[role].label.toUpperCase()}</Text><Text style={s.title}>Activity</Text><Text style={s.sub}>A transparent history of submissions, assignments, and progress updates.</Text></View>} ListHeaderComponentStyle={{ marginBottom: 18 }} renderItem={({ item }) => <View style={s.card}><View style={s.dot}><MaterialIcons name="check" size={13} color="#FFF" /></View><View style={{ flex: 1, gap: 3 }}><View style={s.row}><Text style={s.ref}>{item.request}</Text><Text style={s.time}>{item.time}</Text></View><Text style={s.action}>{item.action}</Text><Text style={s.author}>{item.author}</Text>{item.note && <Text style={s.note}>{item.note}</Text>}</View></View>} /></View>; }
const s = StyleSheet.create({ screen: { flex: 1, backgroundColor: C.bg }, content: { padding: 20, paddingBottom: 34, maxWidth: 780, width: "100%", alignSelf: "center" }, wide: { paddingHorizontal: 28, paddingTop: 28 }, eyebrow: { color: C.blue, fontSize: 10, letterSpacing: 1.2, fontWeight: "900", marginBottom: 6 }, title: { color: C.navy, fontSize: 27, fontWeight: "900" }, sub: { color: C.muted, fontSize: 13, lineHeight: 19, marginTop: 5 }, card: { backgroundColor: C.surface, borderWidth: 1, borderColor: C.border, borderRadius: 18, padding: 15, flexDirection: "row", gap: 12, ...shadow }, dot: { width: 23, height: 23, borderRadius: 12, backgroundColor: C.blue, alignItems: "center", justifyContent: "center" }, row: { flexDirection: "row", justifyContent: "space-between", gap: 8 }, ref: { color: C.blue, fontSize: 11, fontWeight: "900" }, time: { color: C.muted, fontSize: 10, fontWeight: "700" }, action: { color: C.navy, fontSize: 15, fontWeight: "900" }, author: { color: C.muted, fontSize: 12, fontWeight: "700" }, note: { color: C.text, fontSize: 12, lineHeight: 18, backgroundColor: C.bg, padding: 9, borderRadius: 10, marginTop: 3 } });
