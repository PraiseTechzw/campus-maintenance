import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { useRouter as useExpoRouter } from "expo-router";
import { Pressable, ScrollView, StyleSheet, Text, useWindowDimensions, View } from "react-native";
import { Mark } from "@/components/campus-ui";
import { LanguagePicker } from "@/components/language-picker";
import { C, shadow } from "@/constants/campus";
import { useAuth } from "@/hooks/use-auth";
import { roleLabelKey, useI18n } from "@/lib/i18n";
import { useMaintenance } from "@/lib/maintenance-store";
import { trpc } from "@/lib/trpc";

const useRouter = () => useExpoRouter() as any;

export default function Profile() {
  const router = useRouter();
  const { role } = useMaintenance();
  const { t } = useI18n();
  const { width } = useWindowDimensions();
  const desktop = width >= 900;
  const { user, isAuthenticated, loading, logout } = useAuth();
  const live = trpc.maintenance.list.useQuery(undefined, { enabled: isAuthenticated, retry: false });
  const accountName = user?.name?.trim() || user?.email || "Institution account";
  return <View style={s.screen}><ScrollView contentContainerStyle={[s.content, width > 740 && s.wide, desktop && s.desktopContent]}><View style={[s.grid, desktop && s.desktopGrid]}><View style={s.mainColumn}><Mark /><View style={s.hero}><View style={s.initials}><Text style={s.initialsText}>{accountName.split(" ").map((part) => part[0]).join("").slice(0, 2).toUpperCase()}</Text></View><View style={{ flex: 1 }}><Text style={s.name}>{isAuthenticated ? accountName : "Secure access required"}</Text><Text style={s.role}>{isAuthenticated ? t(roleLabelKey[role]) : "Institution account"}</Text><Text style={s.accountStatus}>{isAuthenticated ? "ACCOUNT VERIFIED" : "NOT SIGNED IN"}</Text></View></View><Text style={s.section}>{t("liveAccount")}</Text><View style={s.accountCard}><View style={s.accountIcon}><MaterialIcons name={isAuthenticated ? "verified-user" : "lock-outline"} size={20} color={isAuthenticated ? C.success : C.blue} /></View><View style={{ flex: 1 }}><Text style={s.accountTitle}>{isAuthenticated ? user?.email ?? accountName : "Connect your institution account"}</Text><Text style={s.accountDetail}>{isAuthenticated ? live.isLoading ? "Loading account-backed request data…" : `${t("savedRequests")}: ${live.data?.length ?? 0} · Role permissions active` : "Sign in to access approved locations, account-backed requests, and role permissions."}</Text></View>{isAuthenticated ? <Pressable onPress={() => void logout()} style={s.accountButton}><Text style={s.accountButtonText}>{t("signOut")}</Text></Pressable> : <Pressable onPress={() => router.push("/welcome")} style={s.accountButton}><Text style={s.accountButtonText}>{loading ? "…" : t("signIn")}</Text></Pressable>}</View>{desktop && <View style={s.webAccountContext}><MaterialIcons name="desktop-windows" size={19} color={C.blue} /><View style={{ flex: 1 }}><Text style={s.webAccountTitle}>Website workspace</Text><Text style={s.webAccountText}>Your account, language, notifications, and administrator configuration remain available from this panel on larger screens.</Text></View></View>}</View><View style={s.settingsColumn}><Text style={s.section}>{t("language")}</Text><View style={s.languageCard}><Text style={s.languageCaption}>Choose the language used in your Campus Maintenance workspace.</Text><LanguagePicker /></View><Text style={s.section}>Account settings</Text><View style={s.list}>{role === "administrator" && isAuthenticated && <Row icon="admin-panel-settings" title="Operations configuration" detail="Manage registered accounts, staff roles, verified locations, and SLA targets." onPress={() => router.push("/admin/operations")} />}<Row icon="notifications-none" title={t("notifications")} detail="Set account-level notification preferences." onPress={() => router.push("/settings/notifications")} /><Row icon="help-outline" title={t("helpSupport")} detail="Get support with access, reporting, or service follow-up." onPress={() => router.push("/help")} /></View><View style={s.notice}><MaterialIcons name="shield" size={19} color={C.blue} /><Text style={s.noticeText}>Operational data is available only through authenticated institution accounts. Administrators configure locations, service targets, and staff roles before live work is created.</Text></View></View></View></ScrollView></View>;
}

function Row({ icon, title, detail, onPress }: { icon: keyof typeof MaterialIcons.glyphMap; title: string; detail: string; onPress: () => void }) { return <Pressable onPress={onPress} style={({ pressed }) => [s.row, pressed && s.pressed]}><View style={s.rowIcon}><MaterialIcons name={icon} size={20} color={C.blue} /></View><View style={{ flex: 1 }}><Text style={s.rowTitle}>{title}</Text><Text style={s.rowDetail} numberOfLines={2}>{detail}</Text></View><MaterialIcons name="chevron-right" size={22} color={C.muted} /></Pressable>; }

const s = StyleSheet.create({ screen: { flex: 1, backgroundColor: C.bg }, content: { padding: 20, paddingBottom: 34, maxWidth: 780, width: "100%", alignSelf: "center" }, wide: { paddingHorizontal: 28, paddingTop: 28 }, desktopContent: { maxWidth: 1120, paddingHorizontal: 38, paddingTop: 34 }, grid: { gap: 18 }, desktopGrid: { flexDirection: "row", alignItems: "flex-start", gap: 22 }, mainColumn: { gap: 18, flex: 1 }, settingsColumn: { gap: 15, flex: 1 }, hero: { backgroundColor: C.navy, borderRadius: 22, padding: 20, flexDirection: "row", alignItems: "center", gap: 14, ...shadow }, initials: { width: 52, height: 52, borderRadius: 18, backgroundColor: "#FFF", alignItems: "center", justifyContent: "center" }, initialsText: { color: C.navy, fontWeight: "900", fontSize: 16 }, name: { color: "#FFF", fontSize: 19, fontWeight: "900" }, role: { color: "#BBD8FF", fontSize: 13, fontWeight: "700", marginTop: 2 }, accountStatus: { color: "#7FC0FF", fontSize: 9, letterSpacing: 1, fontWeight: "900", marginTop: 4 }, section: { color: C.navy, fontSize: 17, fontWeight: "900" }, accountCard: { flexDirection: "row", alignItems: "center", gap: 10, padding: 14, borderRadius: 18, borderColor: "#CFE4FC", borderWidth: 1, backgroundColor: C.sky }, accountIcon: { width: 38, height: 38, borderRadius: 12, alignItems: "center", justifyContent: "center", backgroundColor: "#FFFFFF" }, accountTitle: { color: C.navy, fontSize: 13, fontWeight: "900" }, accountDetail: { color: "#31547F", fontSize: 11, lineHeight: 16, marginTop: 2 }, accountButton: { minHeight: 35, justifyContent: "center", paddingHorizontal: 11, borderRadius: 10, backgroundColor: C.navy }, accountButtonText: { color: "#FFFFFF", fontSize: 11, fontWeight: "900" }, webAccountContext: { flexDirection: "row", gap: 9, alignItems: "flex-start", padding: 13, borderRadius: 16, backgroundColor: "#FFF", borderWidth: 1, borderColor: C.border }, webAccountTitle: { color: C.navy, fontSize: 12, fontWeight: "900" }, webAccountText: { color: C.muted, fontSize: 11, lineHeight: 16, marginTop: 2 }, languageCard: { padding: 14, borderRadius: 18, backgroundColor: "#FFF", borderWidth: 1, borderColor: C.border, gap: 9 }, languageCaption: { color: C.muted, fontSize: 11, lineHeight: 16 }, list: { overflow: "hidden", borderRadius: 20, borderColor: C.border, borderWidth: 1, backgroundColor: C.surface }, row: { minHeight: 76, padding: 14, flexDirection: "row", alignItems: "center", gap: 12, borderBottomWidth: 1, borderBottomColor: C.border }, rowIcon: { height: 37, width: 37, borderRadius: 12, backgroundColor: C.sky, alignItems: "center", justifyContent: "center" }, rowTitle: { color: C.navy, fontSize: 14, fontWeight: "900" }, rowDetail: { color: C.muted, fontSize: 12, marginTop: 2 }, notice: { flexDirection: "row", gap: 9, alignItems: "flex-start", padding: 14, borderRadius: 17, backgroundColor: C.sky, borderWidth: 1, borderColor: "#CFE4FC" }, noticeText: { color: "#31547F", fontSize: 12, lineHeight: 18, flex: 1, fontWeight: "600" }, pressed: { opacity: 0.74 } });
